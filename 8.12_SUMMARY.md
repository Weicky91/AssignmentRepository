# ==8.12-F405RTG6:face_with_head_bandage:==

Created: 2025年8月12日 09:01

## ARM ST

1. cortex-M系列用于嵌入式系统
2. 队内使用F405RGT6,H723ZET6(后者性能更好)
3. cortex-M4内核，F系列注重高性能

——使用的是一个ST制造的，内部集成了cortex-M4核心的微控制器

## Keil MDK

- ARM公司开发的IDE，为ARM内核量身定做的官方开发工具
  
    还有cubeIDE IAR等，集成度高，但兼容性不佳
    

## STM32F405

- 在cubeMX上面菜单栏里help-Docs里有很多手册
- 进入Keil之后，软件包什么的下载在Keil内置的Packs里去下，不用担心版本问题什么的

（Keil下载的时候，只能更改第二个存储路径）

- RGT6软件包：包含了hal库开发，开发途径有寄存器，标准库，hal库

### ==F405RGT6框图:exclamation:==

![image.png](image.png)

这张图展示了STM32F405xx和STM32F407xx系列微控制器的多AHB（Advanced High-performance Bus）总线矩阵结构。图中详细描述了不同总线和存储器、外设之间的连接关系。以下是对图中各部分的详细解释：

#### ①顶部模块

1. **64-Kbyte CCM data RAM**：这是一块64KB的代码存储器（CCM），用于存储常量数据，具有快速访问特性。
2. **ARM Cortex-M4**：这是微控制器的核心处理器，ARM Cortex-M4内核。
3. **GP DMA1 和 GP DMA2**：这是两个通用DMA（直接存储器访问）控制器，用于在外设和存储器之间传输数据，减轻CPU负担。
4. **MAC Ethernet**：这是以太网媒体访问控制（MAC）模块，用于处理以太网通信。
5. **USB OTG HS**：这是USB On-The-Go High-Speed模块，支持USB 2.0高速通信。

#### ②中间总线矩阵

- **I-bus（指令总线）**：连接处理器和存储器，用于从存储器中获取指令。
- **D-bus（数据总线）**：连接处理器和存储器，用于数据的读写操作。
- **S-bus（系统总线）**：连接处理器和DMA控制器，用于DMA操作。

#### ③右侧存储器和外设

1. **Flash memory**：这是主闪存存储器，用于存储程序代码。
2. **SRAM1 (112 Kbyte) 和 SRAM2 (16 Kbyte)**：这是两个静态随机存取存储器（SRAM），用于存储运行时数据。
3. **AHB1 peripherals 和 AHB2 peripherals**：这是两组AHB外设接口，用于连接各种外设。
4. **FSMC（灵活静态存储控制器）**：用于连接外部存储器，如SRAM、NOR Flash、NAND Flash、PSRAM等。
5. **Static MemCtl**：静态存储器控制器，用于管理静态存储器。

#### ④底部总线矩阵

- **Bus matrix-S**：这是一个总线矩阵，用于连接不同的总线和外设。它允许多个主设备（如处理器、DMA控制器）和多个从设备（如存储器、外设）之间的灵活连接。

  

## 时钟源——HSE

![image.png](image%201.png)



## EventRecorder

* 调试/计时/实时printf

* Keil>5.40,使用版本AC6

- EventStatics:

```c
while(1){
EventStartA(0);
//your code here;
EventStopA(0);
}
//上面就可以展示code运行的时间
```



## ==问题积累:sailboat:==

### ①连接不上设备

* 在option for target中将Use设置为“CMSIS-DAP Debugger”
* download之后可以进行debug

![image-20250812200827377](C:\Users\LENOVO\AppData\Roaming\Typora\typora-user-images\image-20250812200827377.png)

### ②组件问题

* Device的组件下，没有单独的CubeMX，选择名称类似的“STM32CubeMX”之后出现“Error”——组件无法使用

  > 不知道具体原因，最后通过新建工程解决了<img src="C:\Users\LENOVO\AppData\Roaming\Typora\typora-user-images\image-20250812201159074.png" alt="image-20250812201159074" style="zoom:80%;" />

### ③头文件报问题

* 在教学里指出了解决办法

  >  #include <stdatomic.h>——AC版本：6.22
  >
  > #include CMSIS_device_header——芯片没识别出来——<img src="C:\Users\LENOVO\AppData\Roaming\Typora\typora-user-images\image-20250812201549319.png" alt="image-20250812201549319" style="zoom: 50%;" /><img src="C:\Users\LENOVO\AppData\Roaming\Typora\typora-user-images\image-20250812201521061.png" alt="image-20250812201521061" style="zoom: 50%;" />
  >
  > 

### ④Hal重复获取

* 将新产生的Hal库给隐藏掉（进入option for target 隐藏）

  ![image-20250812201730075](C:\Users\LENOVO\AppData\Roaming\Typora\typora-user-images\image-20250812201730075.png)



## **今天的作业**

**复刻流水灯和蜂鸣器**

```c
#define BEEP_ON HAL_GPIO_WritePin(BEEP_PORT, BEEP_PIN, GPIO_PIN_SET)
#define BEEP_OFF HAL_GPIO_WritePin(BEEP_PORT, BEEP_PIN, GPIO_PIN_RESET)
#define LED_ON HAL_GPIO_WritePin(LED_PORT, LED1_PIN, GPIO_PIN_SET)
#define LED_OFF HAL_GPIO_WritePin(LED_PORT, LED1_PIN, GPIO_PIN_RESET)
```

**工作流程**

1. 在项目Drivers里创建相应个人模块的文件夹，内含个人写的函数和头文件
2. 在Keil里加入到自己的项目里，从而能让项目识别新建C文件，并且能自动识别h文件
3. 编译内容：

```c
#ifndef LED_BEEP_H_
#define LED_BEEP_H_

/*----------------------------------include-----------------------------------*/
#include "gpio.h"
#include "main.h"
#include "stm32f4xx_hal_gpio.h"

#define BEEP_PORT GPIOA
#define LED_PORT  GPIOA

#define LED1_PIN  GPIO_PIN_4
#define LED2_PIN  GPIO_PIN_5
#define LED3_PIN  GPIO_PIN_6
#define LED4_PIN  GPIO_PIN_7
#define BEEP_PIN  GPIO_PIN_8

#define BEEP_ON HAL_GPIO_WritePin(BEEP_PORT, BEEP_PIN, GPIO_PIN_SET)
#define BEEP_OFF HAL_GPIO_WritePin(BEEP_PORT, BEEP_PIN, GPIO_PIN_RESET)

#define LED1_ON HAL_GPIO_WritePin(LED_PORT, LED1_PIN, GPIO_PIN_SET)
#define LED1_OFF HAL_GPIO_WritePin(LED_PORT, LED1_PIN, GPIO_PIN_RESET)

#define LED2_ON HAL_GPIO_WritePin(LED_PORT, LED2_PIN, GPIO_PIN_SET)
#define LED2_OFF HAL_GPIO_WritePin(LED_PORT, LED2_PIN, GPIO_PIN_RESET)

#define LED3_ON HAL_GPIO_WritePin(LED_PORT, LED3_PIN, GPIO_PIN_SET)
#define LED3_OFF HAL_GPIO_WritePin(LED_PORT, LED3_PIN, GPIO_PIN_RESET)

#define LED4_ON HAL_GPIO_WritePin(LED_PORT, LED4_PIN, GPIO_PIN_SET)
#define LED4_OFF HAL_GPIO_WritePin(LED_PORT, LED4_PIN, GPIO_PIN_RESET)

/*functions*/
/*WATER_LED*/
void LED_WATER(void);
/*BEEP_AlARM*/
void BEEP_ALARM(int times);

#endif

```

```c
#include "LED_BEEP.h"

/*WATER_LED*/
void LED_WATER(void)
{
	LED1_ON;
	HAL_Delay(200);
	LED1_OFF;
	LED2_ON;
	HAL_Delay(200);
	LED2_OFF;
	LED3_ON;
	HAL_Delay(200);
	LED3_OFF;
	LED4_ON;
	HAL_Delay(200);
	LED4_OFF;
	HAL_Delay(100);
}
/*BEEP_AlARM*/
void BEEP_ALARM(int times)
{
	while(times>0)
	{
		BEEP_ON;
		HAL_Delay(50);
		times--;
	}
}

```

1. 在main函数里调用就ok了
2. 进行Build，连接JLink

