# 8.15-CAN通讯

Created: 2025年8月15日 09:35

## CAN通讯

- Controller Area Network

<img src="image.png" alt="image.png" style="zoom:33%;" />

- 国际标准化的串行通信协议，采用数据块编码，数据块根据数据帧的类型
- **==差分信号==：根据两根线上的电位差来判断总线电平。分为显性和隐性电平，发送方通过使总线电平发生变化，将消息发送给接收方——由于是差分信号，因此受到干扰时整体一起受到干扰，电平差不变，因此能够远距离传输**
- 显性：电平不同——逻辑0（3.5/1.5V）
  
    隐性：电平相同——逻辑1（2.5/2.5V）
    
    <img src="image%201.png" alt="image.png" style="zoom:80%;" />

<img src="image%202.png" alt="image.png" style="zoom:33%;" />

**阻值很重要！出问题先看阻值（==阻值是用来抗干扰减少回路反射的==）**

### ==仲裁段、控制段、数据段==

<img src="image%203.png" alt="image.png" style="zoom:50%;" />

#### ①仲裁段

- 最核心部分，负责总线访问控制和消息优先级管理

含有ID以及传送类型RTR（数据帧还是远程帧）

- **标识符（ID）**：用于唯一标识一个CAN消息。它可以是标准格式（11位）或扩展格式（29位）。
- **远程传输请求位（RTR）**：只有一个比特，用于区分数据帧和远程帧。RTR为0表示数据帧，RTR为1表示远程帧。
- ==隐性电平遇到显性电平，会被过滤掉==

#### ②控制段

- 控制段包含6个比特的信息，用于描述数据帧的特定属性。
- 它包括数据长度码（DLC），指示数据段中数据的字节数，==范围是0到8个字节。(000→111，八个选择，因此字节数范围在0-8==
- 保留位（r0,r1）必须显性电平发送

#### ③数据段

包含0-8个字节数据（CAN控制器有对应的寄存器，直接写进去就好了）

<img src="image%204.png" alt="image.png" style="zoom:50%;" />

<img src="image%205.png" alt="image.png" style="zoom:50%;" />

- 使用CAN1才能使用CAN2，只有CAN主要的活动内核有==筛选器/过滤器（对于过来的ID有“要求”）==
- ==1. 选择关心的几位——掩码模式==
  
    ![image.png](image%206.png)
    
    ==2.一定要某个ID——列表模式==——接收的ID必须与预设的ID一模一样才能通过过滤，最终被接收到。(2*2=4个预设ID)
    
    ```c
      CAN_FilterInitStructure.CAN_FilterMode = CAN_FilterMode_IdList;    
      CAN_FilterInitStructure.CAN_FilterScale = CAN_FilterScale_16bit;   
      CAN_FilterInitStructure.CAN_FilterIdHigh = std_id<<5;  
      CAN_FilterInitStructure.CAN_FilterIdLow = std_id1<<5;  
      CAN_FilterInitStructure.CAN_FilterMaskIdHigh = std_id<<5;  
      CAN_FilterInitStructure.CAN_FilterMaskIdLow = std_id1<<5;  
      CAN_FilterInitStructure.CAN_FilterFIFOAssignment = CAN_Filter_FIFO0;       
    
    ```
    
    <img src="image%207.png" alt="image.png" style="zoom:50%;" />
    
- 每个过滤器可以提供：1个32位和2个16位过滤器

        可以配置为掩码模式和列表模式

（屏蔽位模式/掩码模式：指定报文标识符的任意位（允许多个ID的意思））

（列表模式只允许一种ID）

<img src="image%208.png" alt="image.png" style="zoom:50%;" />

**如果屏蔽位为1，则对应的ID位必须与标识符寄存器中的位一致；如果屏蔽位为0，则对应的ID位可以是0或1**→==因此全设置为0x000时，则是全通的==

- **①硬件资源充足**：如果硬件资源充足且需要精确控制，可选择标识符列表模式。
  ②**实时性要求高**：如果需要快速处理大量消息且对精度要求不高，可选择屏蔽位模式。
  
    

### CAN的波特率计算

![image.png](image%209.png)

<img src="image%2010.png" alt="image.png" style="zoom:50%;" />

（以示例工程的参数为例）

- APB总线频率：42MHZ
- 分频系数：3
- TBS1：9
- TBS2：4
- SJW：1

42/3/（9+4+1）=1 M

### CAN外设结构

- 邮箱——3*发送邮箱和2*接收邮箱
- 筛选器

## CAN三种调试模式

1. **回环模式LoopBack**：在回环模式下，设备能够发送数据到总线，并且这些数据也会被设备自身接收。这种模式主要用于测试设备的发送和接收功能是否正常工作——==经常用于调试测试==
2. **静默模式Silent：**设备只能发送隐性位（1），不能发送显性位（0）——由于不能发送显性位，因此不会对总线产生影响，监听、分析总线上的数据
3. **静默回环模式Silent_LoopBack：**静默回环模式结合了静默模式和回环模式的特点。自发自接且不影响总线——用于设备的自我检查（热自检）

![image.png](image%2011.png)